var Common = {
  fs: require('fs'),
  db: require('shdb')
};

module.exports = Common;

// usage: var gCommon = require('common.js');
// SWD - not sure I like this - but need something for common includes